apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kmac-2989"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kmac-2989"
  template:
    metadata:
      labels:
        app: "kmac-2989"
    spec:
      containers:
        - name: "kmac-2989"
          image: "encoderbot.azurecr.io/kmac"
          ports:
          - containerPort: 80