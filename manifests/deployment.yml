apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kmac-831f"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kmac-831f"
  template:
    metadata:
      labels:
        app: "kmac-831f"
    spec:
      containers:
        - name: "kmac-831f"
          image: "encoderbot.azurecr.io/kmac"
          ports:
          - containerPort: 80