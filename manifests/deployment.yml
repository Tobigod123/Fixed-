apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kmac-33fe"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kmac-33fe"
  template:
    metadata:
      labels:
        app: "kmac-33fe"
    spec:
      containers:
        - name: "kmac-33fe"
          image: "encoderbot.azurecr.io/kmac"
          ports:
          - containerPort: 80