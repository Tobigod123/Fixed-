apiVersion: v1
kind: Service
metadata:
    name: "kmac-e8dd"
    labels:
        app: "kmac-e8dd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-e8dd"