apiVersion: v1
kind: Service
metadata:
    name: "kmac-33fe"
    labels:
        app: "kmac-33fe"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-33fe"