apiVersion: v1
kind: Service
metadata:
    name: "kmac-f8cd"
    labels:
        app: "kmac-f8cd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-f8cd"