apiVersion: v1
kind: Service
metadata:
    name: "kmac-1666"
    labels:
        app: "kmac-1666"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-1666"