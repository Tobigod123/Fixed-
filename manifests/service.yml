apiVersion: v1
kind: Service
metadata:
    name: "kmac-ec4c"
    labels:
        app: "kmac-ec4c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-ec4c"