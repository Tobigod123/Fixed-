apiVersion: v1
kind: Service
metadata:
    name: "kmac-2989"
    labels:
        app: "kmac-2989"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-2989"