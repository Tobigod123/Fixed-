apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kmac-f6d5"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kmac-f6d5"
  template:
    metadata:
      labels:
        app: "kmac-f6d5"
    spec:
      containers:
        - name: "kmac-f6d5"
          image: "encoderbot.azurecr.io/kmac"
          ports:
          - containerPort: 80